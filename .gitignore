# build output
Tab5 3D Render/.pio/
**/__pycache__/
meshscan/unitv/build/

# screenshots / captures (regenerate at will)
**/tools/*.bmp
**/tools/*.png

# session-local data
.claude/

# secrets — never commit
**/wifi_creds.h

# raw photo dumps stay local; only curated assets get committed
assets/photos/raw/

# internal working notes (kept local + in platypus-lab history, not in the contest repo)
notes/
